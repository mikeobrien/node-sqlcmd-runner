var expect = require('chai').expect,
    cases = require('cases'),
    parse = require('../src/parser');

describe('parser', function(){

    it('should', function() {
        expect(1).to.equal(1);
    });

});